import screenController from '../../containers/screenController';
export default () => {
  const main = {
    init() {},
    cacheDOM() {},
    bindEvents() {},
    render() {},
  };

  return main.render();
};
